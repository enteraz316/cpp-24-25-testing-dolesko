using String = std::string;



// class "String" implementation
#pragma once

#include <algorithm>
#include <cstring>
#include <iostream>

// class String {};

// if operator[] non-defined:
/*
char& operator[](size_t index) { return (this->data())[index]; }

const char& operator[](size_t index) const { return (this->data())[index]; }
*/